{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm turbo run build --filter=@agora/web",
  "installCommand": "pnpm install --frozen-lockfile=false",
  "framework": "nextjs",
  "outputDirectory": "apps/web/.next"
}
